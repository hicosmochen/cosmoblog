# C语言基础语法（一）







[[TOC]]













## 第一章  第一行代码

### 第01节 案例代码

```c
/**
    第一步 需要先在控制台中执行下面的代码, 进行编译操作
    gcc studyCDemo.c -o studyCDemo
    第二步 需要在控制台中执行下面的代码, 进行运行的操作
    studyCDemo
 */
#include "stdio.h"
int main(){

    printf("welcome to c language world");

    return 0;
}
```





### 第02节 知识说明

main 函数

```
1、一个完整的C语言程序，是由一个、且只能有一个main()函数(又称主函数，必须有)和若干个其他函数结合而成（可选）。
2、main函数是C语言程序的入口，程序是从main函数开始执行。
```

其他函数

```
从函数定义的角度来看，函数可以分为 "系统函数" 和 "用户函数"
1、系统函数: 即库函数：这是由编译系统提供的，用户不必自己定义这些函数，可以直接使用它们，如我们常用的打印函数printf()。
2、用户函数: 用以解决用户的专门需要。
```

头文件

```
1、为什么需要有头文件?
    使用C语言库函数需要提前包含库函数对应的头文件，如这里使用了printf()函数，需要包含stdio.h头文件
    
2、头文件如何使用?
    写法一:  include< > 表示系统直接按系统指定的目录检索
    写法二:  include"" 表示系统先在 "" 指定的路径(没写路径代表当前路径)查找头文件，如果找不到，再按系统指定的目录检索
```

return 语句

```
1、什么是 return 语句?
    return代表函数执行完毕，返回return代表函数的终止
    
2、不同 return 值的含义
    在main函数中 return 0代表程序执行成功，return -1代表程序执行失败
```





### 第03节 语言风格

标识符命名规则

```
1、标识符不能是关键字。
2、标识符只能由字母、数字、下划线组成。
3、第一个字符必须为字母或下划线。
4、标识符中字母区分大小写。
```

注释

```
1、 /叫行注释，注释的内容编译器是忽略的，注释主要的作用是在代码中加一些说明和解释，这样有利于代码的阅读
2、 /**/叫块注释
```





### 第04节 关键字

在 C 语言中, 关键字一共有 32个

数据类型的关键字 12个

```c
char  		short  		int  	long  		float  		double
unsigned    signed 		struct	 union		 enum		void
```

流程控制关键字  12个

```c
if		else		switch		case 		default			goto
for		do			while		break		continue		return
```

存储类型的关键字 5个

```c
auto	extern		register	static		const
```

其他关键字

```c
sizeof		typedef		volatile
```







### 第05节 常量变量

常量

```
1、在程序运行的过程中, 其值不能被改变的量
2、常量一般出现在表达式或者赋值语句中
```

| 整型常量   | 100，200，-100，0     |
| ---------- | --------------------- |
| 实型常量   | 3.14 ， 0.125，-3.123 |
| 字符型常量 | ‘a’,‘b’,‘1’,‘\n’      |
| 字符串常量 | “a”,“ab”，“12356”     |



变量

```
1、在程序运行的过程中, 其值可以改变
2、变量使用之前, 必须先定义, 定义变量必须有相应的数据类型
```





















## 第二章 数据类型

### 第01节 基础描述

数据类型有什么作用？

```
编译器 预算对象（变量）分配的内存空间大小。
```

数据类型分类

```
1、基本类型
	A. 整型		 int、short、long
	B. 字符型		char
	C. 浮点型		float、double

2、构造类型
	A. 数组类型
	B. 结构类型  struct
	C. 联合类型  union
	D. 枚举类型  enum

3、指针类型
	A. 字符指针 char*
	B. 整型指针 int*
	..........
```





### 第02节 整型使用

整型的定义和输出

| 打印格式  | 含义                                |
| --------- | ----------------------------------- |
| %d        | 输出一个有符号的10进制int类型       |
| %o(字母o) | 输出8进制的int类型                  |
| %x        | 输出16进制的int类型，字母以小写输出 |
| %X        | 输出16进制的int类型，字母以大写输出 |
| %u        | 输出一个10进制的无符号数            |

案例代码  **整型变量的输出**

```c
/**
    第一步 需要先在控制台中执行下面的代码, 进行编译操作
    gcc studyCDemo.c -o studyCDemo
    第二步 需要在控制台中执行下面的代码, 进行运行的操作
    studyCDemo
 */
#include "stdio.h"
int main(){
    int a = 123;     // 定义变量 a, 以10进制方式, 赋值为 123
    int b = 0567;    // 定义变量 b, 以8进制方式, 赋值为 0567
    int c = 0xabc;   // 定义变量 c, 以16进制方式, 赋值为 0xabc

    printf("a = %d \n", a);
    printf("8 进制: b = %o \n", b);
    printf("10 进制: b = %d \n", b);
    printf("16 进制: c = %x \n", c);
    printf("16 进制: c = %X \n", c);
    printf("10 进制: c = %d \n", c);
	unsigned int d = 0xFFFFFFFF;   // 定义无符号int变量d, 以16进制方式, 赋值
	printf("有符号的方式打印: d = %d \n", d);
	printf("无符号的方式打印: d = %u \n", d);
    return 0;
}
```

运行结果

> a = 123
>
> 8 进制: b = 567
>
> 10 进制: b = 375
>
> 16 进制: c = abc
>
> 16 进制: c = ABC
>
> 10 进制: c = 2748
>
> 有符号的方式打印: d = -1
>
> 无符号的方式打印: d = 4294967295



案例代码 **整型变量的输入**

```c
/**
    第一步 需要先在控制台中执行下面的代码, 进行编译操作
    gcc studyCDemo.c -o studyCDemo
    第二步 需要在控制台中执行下面的代码, 进行运行的操作
    studyCDemo
 */
#include "stdio.h"
int main(){
	int a;
	printf("请输入 a的值: ");
	scanf("%d" , &a);
	printf("a = %d \n", a);   // 打印a的值
    return 0;
}
```

运行结果

> 请输入 a的值: 12
>
> a = 12





**其他整型 short、int、long、longlong** 

| 数据类型            | 占用空间                                        |
| ------------------- | ----------------------------------------------- |
| short(短整型)       | 2字节                                           |
| int(整型)           | 4字节                                           |
| long(长整形)        | Windows为4字节，Linux为4字节(32位)，8字节(64位) |
| long long(长长整形) | 8字节                                           |

注意事项

```
需要注意的是，整型数据在内存中占的字节数与所选择的操作系统有关。

1、虽然 C 语言标准中没有明确规定整型数据的长度，但 long 类型整数的长度不能短于 int 类型， short 类型整数的长度不能长于 int 类型。
2、当一个小的数据类型赋值给一个大的数据类型，不会出错，因为编译器会自动转化。
3、但当一个大的类型赋值给一个小的数据类型，那么就可能丢失高位。
```







### 第03节 字符使用

描述

```
1、字符类型变量, 用于存储一个单一字符。
2、在 C语言当中, 采用 char 表示
3、每一个字符都会占用 1个字节。
4、在给字符变量赋值的时候, 都需要采用 英文的单引号括起来。

字符变量实际上并不是把该字符本身放到变量的内存单元中去, 而是将该字符对应的 ASCII 编码放到变量的存储单元中。
char 的本质就是一个 1字节大小的整型
```

案例代码 **字符变量的输出**

```c
/**
    第一步 需要先在控制台中执行下面的代码, 进行编译操作
    gcc studyCDemo.c -o studyCDemo
    第二步 需要在控制台中执行下面的代码, 进行运行的操作
    studyCDemo
 */
#include "stdio.h"
int main(){
	
	char ch = 'a';
	printf("sizeof(ch) = %u \n" , sizeof(ch));
	
	printf("ch[%%c] = %c \n" , ch);  // 打印字符
	printf("ch[%%d] = %d \n" , ch);  // 打印'a' ASCII的值
	
	char A = 'A';
	char a = 'a';
	printf("a = %d \n", a);
	printf("A = %d \n", A);
	
	printf("A = %c \n", 'a'-32);   // 小写a转大写A
	printf("a = %c \n", 'A'+32);   // 大写A转小写a
	
	ch = ' ';
	printf("空字符: %d \n", ch);    // 空字符 ASCII的值 为 32
	printf("A = %c \n", 'a'-' ');   // 小写 a 转大写 A
	printf("a = %c \n", 'A'+' ');   // 大写 A 转小写 a
	
    return 0;
}
```

运行结果

>sizeof(ch) = 1
>
>ch[%c] = a
>
>ch[%d] = 97
>
>a = 97
>
>A = 65
>
>A = A
>
>a = a
>
>空字符: 32
>
>A = A
>
>a = a





案例代码 **字符变量的输入**

```c
/**
    第一步 需要先在控制台中执行下面的代码, 进行编译操作
    gcc studyCDemo.c -o studyCDemo
    第二步 需要在控制台中执行下面的代码, 进行运行的操作
    studyCDemo
 */
#include "stdio.h"
int main(){
	
	char ch;
	printf("请输入 ch的值: ");
	scanf("%c", &ch);
	printf("ch = %c \n", ch);  // 打印ch的字符
	
    return 0;
}
```

运行结果

>请输入 ch的值: k
>
>ch = k

 







### 第04节 浮点使用

描述

```
1、浮点型变量也称为 实型变量。
2、浮点型变量是用来存储小数数值的。
3、C语言中, 浮点型变量分为两种: 单精度浮点数（float） 和 双精度浮点数（double） 
4、双进度浮点数表示的比单精度浮点数 更加精确

由于浮点型变量是由有限的存储单元组成的，因此只能提供有限的有效数字。
在有效位以外的数字将被舍去，这样可能会产生一些误差。

不以f结尾的常量是double类型，以f结尾的常量(如3.14f)是float类型。
```

| 数据类型 | 占用空间 | 有效数字范围     |
| -------- | -------- | ---------------- |
| float    | 4字节    | 31位有效数字     |
| double   | 8字节    | 63～64位有效数字 |



案例代码 **浮点变量的使用**

```c
/**
    第一步 需要先在控制台中执行下面的代码, 进行编译操作
    gcc studyCDemo.c -o studyCDemo
    第二步 需要在控制台中执行下面的代码, 进行运行的操作
    studyCDemo
 */
#include "stdio.h"
int main(){
	
	// 传统方式赋值
	float a = 3.14F;    // 或者 3.14f
	double b = 3.14;
	
	printf("a = %f \n", a);
	printf("b = %f \n", b);
	
	// 科学法赋值
	a = 32e3f;    // 3.2 * 1000 = 3200  e也可以写成E
	printf("a1 = %f \n", a);
	
	a = 100e-3f;  // 100*0.001 = 0.1
	printf("a2 = %f \n", a);
	
	a = 3.1415926f;
	printf("a3 = %f \n", a);
	
    return 0;
}
```

运行结果

>a = 3.140000
>
>b = 3.140000
>
>a1 = 32000.000000
>
>a2 = 0.100000
>
>a3 = 3.141593

 















## 第三章 运算符

### 第01节 常用运算符

常用运算符的分类

| 运算符类型    | 作用                                   |
| ------------- | -------------------------------------- |
| 算术运算符    | 用于处理四则运算                       |
| 赋值运算符    | 用于将表达式的值赋给变量               |
| 比较运算符    | 用于表达式的比较，并返回一个真值或假值 |
| 逻辑运算符    | 用于根据表达式的值返回真值或假值       |
| 位运算符      | 用于处理数据的位运算                   |
| sizeof 运算符 | 用于求字节数长度                       |



**算术运算符**

| 运算符 | 术语         | 示例         | 结果       |
| ------ | ------------ | ------------ | ---------- |
| +      | 正号         | +3           | 3          |
| -      | 负号         | -3           | -3         |
| +      | 加           | 10+5         | 15         |
| -      | 减           | 10-5         | 5          |
| *      | 乘           | 10* 5        | 50         |
| /      | 除           | 10/5         | 2          |
| %      | 取模（取余） | 10%3         | 1          |
| ++     | 前自增       | a=2;  b=++a; | a=3;  b=3; |
| ++     | 后自增       | a=2;  b=a++; | a=3;  b=2; |
| --     | 前自减       | a=2;  b=--a; | a=1;  b=1; |
| --     | 后自减       | a=2;  b=a--; | a=1;  b=2; |



**赋值运算符**

| 运算符 | 术语   | 示例         | 结果        |
| ------ | ------ | ------------ | ----------- |
| =      | 赋值   | a=2;   b=3;  | a=2;   b=3; |
| +=     | 加等于 | a=0;   a+=2; | a=2;        |
| -=     | 减等于 | a=5;   a-=3; | a=2;        |
| *=     | 乘等于 | a=2;   a*=2; | a=4;        |
| /=     | 除等于 | a=4;   a/=2; | a=2;        |
| %=     | 模等于 | a=3;   a%=2; | a=1;        |



**比较运算符**

在 C语言 中, 比较运算符结果。 “真” 用数字 “1” 来表示。 “假” 用数字 “0” 来表示。

| 运算符 | 术语     | 示例   | 结果 |
| ------ | -------- | ------ | ---- |
| ==     | 相等     | 4 == 3 | 0    |
| !=     | 不相等   | 4 != 3 | 1    |
| <      | 小于     | 4 < 3  | 0    |
| >      | 大于     | 4 > 3  | 1    |
| <=     | 小于等于 | 4 <= 3 | 0    |
| >=     | 大于等于 | 4 >= 3 | 1    |



**逻辑运算符**

| 运算符 | 术语 | 示例     | 结果                                                        |
| ------ | ---- | -------- | ----------------------------------------------------------- |
| !      | 非   | !a       | 如果 a 为假， 则 !a 为真。   如果 a 为真， 则 !a 为假       |
| &&     | 与   | a && b   | 如果 a 和 b 都为真，则结果为真，否则为假                    |
| \|\|   | 或   | a \|\| b | 如果 a 和 b 有一个为真，则结果为真。 二者都为假时，结果为假 |







### 第02节 类型转换

描述： 数据有不同的类型，不同类型数据之间进行混合运算时必然涉及到类型的转换问题。

```
转换的方法有两种：
    1、自动转换(隐式转换)： 遵循一定的规则,由编译系统自动完成。
    2、强制类型转换： 把表达式的运算结果强制转换成所需的数据类型。
    
类型转换的原则：占用内存字节数少(值域小)的类型，向占用内存字节数多(值域大)的类型转换，以保证精度不降低。
```

类型转换的顺序表

| 最低          | 低         | 中            | 高   | 较高  | 最高   |
| ------------- | ---------- | ------------- | ---- | ----- | ------ |
| char 和 short | signed int | unsigned  int | long | float | double |



案例代码 **自动类型转换**

```c
/**
    第一步 需要先在控制台中执行下面的代码, 进行编译操作
    gcc studyCDemo.c -o studyCDemo
    第二步 需要在控制台中执行下面的代码, 进行运行的操作
    studyCDemo
 */
#include "stdio.h"
int main(){
	
	int num = 5;
	
	printf("s1 = %d \n", num/2);
	printf("s2 = %lf \n", num/2.0);
	
    return 0;
}
```

运行结果

>s1 = 2
>
>s2 = 2.500000





案例代码 **强制类型转换**

强制类型转换指的是使用强制类型转换运算符，将一个变量或表达式转化成所需的类型

```c
/**
    第一步 需要先在控制台中执行下面的代码, 进行编译操作
    gcc studyCDemo.c -o studyCDemo
    第二步 需要在控制台中执行下面的代码, 进行运行的操作
    studyCDemo
 */
#include "stdio.h"
int main(){
	
	float x = 0;
	int i = 0;
	x = 3.6F;
	
	i = x;    // x 为浮点型, i为整型。 直接赋值会有警告。
	i = (int) x;    // 使用强制类型转换
	
	printf("x = %f, i = %d \n", x, i);
	
    return 0;
}
```

运行结果

> x = 3.600000, i = 3



















## 第四章  输出输入

### 第01节 输出函数

描述

```
函数:
	printf()
	
作用:
	输出一个字符串	
```

格式字符串

| 打印格式 | 对应数据类型    | 含义                                                         |
| -------- | --------------- | ------------------------------------------------------------ |
| %d       | int             | 接收整数值 并且 将它表示为 有符号的 十进制整数               |
| %hd      | short   int     | 短整数                                                       |
| %hu      | unsigned  short | 无符号 短整数                                                |
| %o       | unsigned  int   | 无符号 8进制整数                                             |
| %u       | unsigned  int   | 无符号 10进制整数                                            |
| %x,  %X  | unsigned  int   | 无符号 16进制整数， x 对应的是 abcdef，    X 对应的是 ABCDEF |
| %f       | float           | 单精度浮点数                                                 |
| %lf      | double          | 双精度浮点数                                                 |
| %e,  %E  | double          | 科学计数法表示的数，此处 "e" 的大小写代表输出时，用的 "e"的大小写 |
| %c       | char            | 字符型，可以把输入的数字按照 ASCII 码相应转换为对应的字符    |
| %s       | char*           | 字符串，输出字符串中字符直至字符串中空字符（字符串以 '\0' 结尾，这个 '\0' 表示空字符） |
| %p       | void*           | 以 16 进制形式输出指针                                       |
| %%       | %               | 输出一个百分号                                               |



printf 附加格式

| 字符                | 含义                                                         |
| ------------------- | ------------------------------------------------------------ |
| l  (字母 l)         | 附加在 d, u, x, o 前面，表示长整数                           |
| -                   | 左对齐                                                       |
| m (代表一个整数)    | 数据最小宽度                                                 |
| 0 （数字0）         | 将输出的前面补上 0 直到占满指定列宽为止，不可以搭配使用      |
| m.n  (代表一个整数) | m 指域宽，即对应的输出项在输出设备上所占的字符数。 n指精度，用于说明输出的浮点数的小数位数。对数值型来说，未指定时，隐含精度为 n=6 位。 |



案例代码 **输出函数**

```c
/**
    第一步 需要先在控制台中执行下面的代码, 进行编译操作
    gcc studyCDemo.c -o studyCDemo
    第二步 需要在控制台中执行下面的代码, 进行运行的操作
    studyCDemo
 */
#include "stdio.h"
int main(){
	
	int a = 100;
	printf("a = %d \n", a);    // 格式化输出一个字符串
	printf("%p \n", a);    	   // 输出变量 a 在内存中的地址编号
	printf("%%d \n");    	  
	char c = 'a';
	putchar(c);				// putchar 只有一个参数, 就是要输出的 char
	long a2 = 100;
	printf("%ld, %lx, %lo \n", a2, a2, a2);
	long long a3 = 1000;
	printf("%lld, %llx, %llo \n",  a3, a3, a3);
	int abc = 10;
	printf("abc = '%6d' \n", abc);
	printf("abc = '%-6d' \n", abc);
	printf("abc = '%06d' \n", abc);
	printf("abc = '%-06d' \n", abc);
	double d = 12.3;
	printf("d = \'%-10.3lf\' \n", d);
	
    return 0;
}
```

运行结果

> a = 100
>
> 0000000000000064
>
> %d
>
> a100, 64, 144
>
> 1000, 3e8, 1750
>
> abc = '    10'
>
> abc = '10    '
>
> abc = '000010'
>
> abc = '10    '
>
> d = '12.300    '











### 第02节 输入函数

描述

```
函数 
	scanf()
	
作用
	通过 %转义 的方式, 可以得到用户通过标准输入设备输入的数据。
```

案例代码 **输入函数**

```c
/**
    第一步 需要先在控制台中执行下面的代码, 进行编译操作
    gcc studyCDemo.c -o studyCDemo
    第二步 需要在控制台中执行下面的代码, 进行运行的操作
    studyCDemo
 */
#include "stdio.h"
int main(){
	
	char ch1;
	char ch2;
	char ch3;
	int  a;
	int  b;
	
	printf("请输入ch1的字符: ");
	ch1 = getchar();
	printf("ch1 = %c \n", ch1);
	
	getchar();   // 测试此处 getchar() 的作用
	
	printf("请输入ch2的字符: ");
	ch2 = getchar();
	printf("\'ch2 = %ctest\' \n", ch2);
	
	getchar();   // 测试此处 getchar() 的作用
	
	printf("请输入ch3的字符: ");
	scanf("%c" , &ch3);
	printf("\'ch3 = %c\' \n", ch3);
	printf("请输入a的值: ");
	scanf("%d", &a);
	printf("a = %d \n", a);
	printf("请输入b的值: ");
	scanf("%d", &b);
	printf("b = %d \n", b);
	
    return 0;
}
```

运行结果

> 请输入ch1的字符: k
>
> ch1 = k
>
> 请输入ch2的字符: p
>
> 'ch2 = ptest'
>
> 请输入ch3的字符: n
>
> 'ch3 = n'
>
> 请输入a的值: 10
>
> a = 10
>
> 请输入b的值: 20
>
> b = 20























