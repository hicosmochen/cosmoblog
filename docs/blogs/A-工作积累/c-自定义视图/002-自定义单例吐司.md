# 自定义单例吐司



[[TOC]]





## 第一章 效果演示

### 第01节 效果图



<img src="https://cosmocheng.oss-cn-chengdu.aliyuncs.com/study/A-%E5%B7%A5%E4%BD%9C%E7%A7%AF%E7%B4%AF/c-%E8%87%AA%E5%AE%9A%E4%B9%89%E8%A7%86%E5%9B%BE/Snipaste_2023-09-20_19-34-28.png">







### 第02节 主要文件

```
1. 自定义的 Toast 类
2. Activity 类
3. Activity 布局
```





### 第03节 修改项

```java
private interface ToastParam {
    // 默认字体的大小 Toast字体大小
    float defaultTextSize = 14;
    // Toast字体颜色
    int defaultTextColor = 0XFFFFFFFF;
    // Toast背景颜色
    int defaultBgColor = 0XE6F5695A;
    // Toast的高度(单位dp)
    float defaultToastHeight = 50.0F;

    // Toast的位置定义
    int toastLayoutGravity = Gravity.TOP;
    // x 轴方向的偏移量
    int toastLayoutOfferX = 0;
    // y 轴方向的偏移量
    int toastLayoutOfferY = 0;
}
```







## 第二章 案例代码

### 第01节 自定义 Toast 类

```java

import android.content.Context;
import android.util.DisplayMetrics;
import android.view.Gravity;
import android.view.View;
import android.view.ViewGroup;
import android.view.WindowManager;
import android.widget.LinearLayout;
import android.widget.TextView;
import android.widget.Toast;

public class CommonToast {

    private static Context mContext;
    private volatile static CommonToast mInstance;
    private static Toast mToast;
    private View layout;
    private TextView viewText;

    private final LinearLayout.LayoutParams rootParams;
    private final LinearLayout.LayoutParams viewChildParams;

    private interface ToastParam {
        // 默认字体的大小 Toast字体大小
        float defaultTextSize = 14;
        // Toast字体颜色
        int defaultTextColor = 0XFFFFFFFF;
        // Toast背景颜色
        int defaultBgColor = 0XE6F5695A;
        // Toast的高度(单位dp)
        float defaultToastHeight = 50.0F;

        // Toast的位置定义
        int toastLayoutGravity = Gravity.TOP;
        // x 轴方向的偏移量
        int toastLayoutOfferX = 0;
        // y 轴方向的偏移量
        int toastLayoutOfferY = 0;
    }


    private CommonToast(Context context) {
        mContext = context;
        rootParams = new LinearLayout.LayoutParams(getScreenWidth(mContext), ViewGroup.LayoutParams.WRAP_CONTENT);
        viewChildParams = new LinearLayout.LayoutParams(getScreenWidth(mContext), dp2px(ToastParam.defaultToastHeight));
    }


    // 单例模式
    public static CommonToast init(Context context) {
        if (mInstance == null) {
            synchronized (CommonToast.class) {
                if (mInstance == null) {
                    mInstance = new CommonToast(context.getApplicationContext());
                }
            }
        }
        return mInstance;
    }

    private static void getToast() {
        if (mToast == null) {
            mToast = new Toast(mContext);
            // 这里可以设置显示的位置 int gravity, int xOffset, int yOffset
            mToast.setGravity(ToastParam.toastLayoutGravity, ToastParam.toastLayoutOfferX, ToastParam.toastLayoutOfferY);
            //设置显示时间
            mToast.setDuration(Toast.LENGTH_SHORT);
        }
    }


    public void show(String text) {
        makeText(mContext, text, 0);
    }


    private void makeText(Context context, String text, int resourceImageId) {
        init(context);
        getToast();
        if (mInstance.layout == null || mInstance.viewText == null) {
            LinearLayout container = new LinearLayout(mContext);
            container.setLayoutParams(rootParams);
            container.setBackgroundColor(ToastParam.defaultBgColor);
            container.setGravity(Gravity.CENTER);

            mInstance.viewText = new TextView(mContext);
            mInstance.viewText.setLayoutParams(viewChildParams);
            mInstance.viewText.setPadding(dp2px(5), dp2px(2), dp2px(5), dp2px(2));
            mInstance.viewText.setGravity(Gravity.CENTER);
            mInstance.viewText.setTextColor(ToastParam.defaultTextColor);
            mInstance.viewText.setMaxLines(2);
            mInstance.viewText.setTextSize(ToastParam.defaultTextSize);

            container.addView(mInstance.viewText);
            mInstance.layout = container;
            mToast.setView(mInstance.layout);
        }

        mInstance.viewText.setText(text);
        mToast.show();
    }

    // dp转px
    private static int dp2px(float value) {
        float scale = mContext.getResources().getDisplayMetrics().density;
        return (int) (value * scale + 0.5f);
    }

    // 获得屏幕宽度
    private static int getScreenWidth(Context context) {
        WindowManager wm = (WindowManager) context.getSystemService(Context.WINDOW_SERVICE);
        DisplayMetrics outMetrics = new DisplayMetrics();
        wm.getDefaultDisplay().getMetrics(outMetrics);
        float density = outMetrics.density;
        return (int) (outMetrics.widthPixels * density);
    }
}

```





### 第02节 布局文件

```xml
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <TextView
        android:id="@+id/textview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="Test - Demo" />

</LinearLayout>
```





### 第03节 Activity 类

```java
public class DemoTestActivity extends AppCompatActivity {

    private TextView textview;
    private Context context = DemoTestActivity.this;

    int count = 1;

    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_demo_test);

        textview = findViewById(R.id.textview);


        textview.setOnClickListener(v -> {
            
            CommonToast.init(context).show("hello -> " + count);

            count++;
        });
    }
}
```















