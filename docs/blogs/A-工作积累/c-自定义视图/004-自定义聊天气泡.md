# 自定义聊天气泡



[[TOC]]







## 第一章 效果展示

### 第01节 效果图

<img src="https://cosmocheng.oss-cn-chengdu.aliyuncs.com/study/A-%E5%B7%A5%E4%BD%9C%E7%A7%AF%E7%B4%AF/c-%E8%87%AA%E5%AE%9A%E4%B9%89%E8%A7%86%E5%9B%BE/Snipaste_2023-11-02_11-07-53.png">





### 第02节 核心文件

```
一、Java类文件
	1. 自定义的 气泡类, 他是一个继承 FrameLayout 的布局文件
	2. 显示效果的 Activity 类
	
二、layout 布局文件
	1. Activity 使用的布局文件, 存放自定义的气泡布局
	
三、attrs 自定义属性文件
	1. 需要自定义需要气泡携带的一些属性, 可以对气泡进行属性控制
```

> 需要注意的问题：
>
> 1、如果想要 气泡的三角和阴影生效, 必须设置足够的 padding
>
> 2、气泡的方向可以设置左上右下。分别对应属性  `left` 、`top`、`right`、`bottom`
>
> 3、气泡可以设置偏移量。属性为 `bubble_offset` 如果数值为 0 表示在某个方向下居中展示。数值可以为正数 或者 负数
>
> 4、气泡可以设置 阴影 和 圆角









## 第二章 案例代码

### 第01节 自定义气泡布局

位置

```
com.cosmo.good.study.common.view.CommonBubbleLayout
```

代码

```java
import android.content.Context;
import android.content.res.TypedArray;
import android.graphics.Canvas;
import android.graphics.Color;
import android.graphics.Paint;
import android.graphics.Path;
import android.graphics.Point;
import android.graphics.RectF;
import android.util.AttributeSet;
import android.util.TypedValue;
import android.widget.FrameLayout;

import androidx.annotation.IntDef;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

import com.cosmo.good.study.R;

public class CommonBubbleLayout extends FrameLayout {
    public static final int LEFT = 1;
    public static final int TOP = 2;
    public static final int RIGHT = 3;
    public static final int BOTTOM = 4;

    @IntDef({LEFT, TOP, RIGHT, BOTTOM})
    private @interface Direction {
    }

    /**
     * 圆角大小
     */
    private int mRadius;

    /**
     * 三角形的方向
     */
    @Direction
    private int mDirection;

    /**
     * 三角形的底边中心点
     */
    private Point mDatumPoint;

    /**
     * 三角形位置偏移量(默认居中)
     */
    private int mOffset;

    private Paint mBorderPaint;

    private Path mPath;

    private RectF mRect;

    public CommonBubbleLayout(@NonNull Context context) {
        this(context, null);
    }

    public CommonBubbleLayout(Context context, @Nullable AttributeSet attrs) {
        this(context, attrs, 0);
    }

    public CommonBubbleLayout(@NonNull Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        this(context, attrs, defStyleAttr, 0);
    }

    public CommonBubbleLayout(@NonNull Context context, @Nullable AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
        initView(context, attrs, defStyleAttr, defStyleRes);
    }

    private void initView(Context context, AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        TypedArray typedArray = context.obtainStyledAttributes(attrs, R.styleable.BubbleLayout);
        //背景颜色
        int backGroundColor = typedArray.getColor(R.styleable.BubbleLayout_bubble_background_color, Color.WHITE);
        //阴影颜色
        int shadowColor = typedArray.getColor(R.styleable.BubbleLayout_bubble_shadow_color, Color.parseColor("#999999"));
        int defShadowSize = (int) TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_PX, 4, getResources().getDisplayMetrics());
        //阴影尺寸
        int shadowSize = typedArray.getDimensionPixelSize(R.styleable.BubbleLayout_bubble_shadow_size, defShadowSize);
        mRadius = typedArray.getDimensionPixelSize(R.styleable.BubbleLayout_bubble_radius, 0);
        //三角形方向
        mDirection = typedArray.getInt(R.styleable.BubbleLayout_bubble_direction, BOTTOM);
        mOffset = typedArray.getDimensionPixelOffset(R.styleable.BubbleLayout_bubble_offset, 0);
        typedArray.recycle();

        mBorderPaint = new Paint();
        mBorderPaint.setAntiAlias(true);
        mBorderPaint.setColor(backGroundColor);
        mBorderPaint.setShadowLayer(shadowSize, 0, 0, shadowColor);

        mPath = new Path();
        mRect = new RectF();
        mDatumPoint = new Point();

        setWillNotDraw(false);
        //关闭硬件加速
        setLayerType(LAYER_TYPE_SOFTWARE, null);
    }

    @Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);
        if (mDatumPoint.x > 0 && mDatumPoint.y > 0)
            switch (mDirection) {
                case LEFT:
                    drawLeftTriangle(canvas);
                    break;
                case TOP:
                    drawTopTriangle(canvas);
                    break;
                case RIGHT:
                    drawRightTriangle(canvas);
                    break;
                case BOTTOM:
                    drawBottomTriangle(canvas);
                    break;
            }
    }

    private void drawLeftTriangle(Canvas canvas) {
        int triangularLength = getPaddingLeft();
        if (triangularLength == 0) {
            return;
        }

        mPath.addRoundRect(mRect, mRadius, mRadius, Path.Direction.CCW);
        mPath.moveTo(mDatumPoint.x, mDatumPoint.y - triangularLength / 2);
        mPath.lineTo(mDatumPoint.x - triangularLength / 2, mDatumPoint.y);
        mPath.lineTo(mDatumPoint.x, mDatumPoint.y + triangularLength / 2);
        mPath.close();
        canvas.drawPath(mPath, mBorderPaint);
    }

    private void drawTopTriangle(Canvas canvas) {
        int triangularLength = getPaddingTop();
        if (triangularLength == 0) {
            return;
        }

        mPath.addRoundRect(mRect, mRadius, mRadius, Path.Direction.CCW);
        mPath.moveTo(mDatumPoint.x + triangularLength / 2, mDatumPoint.y);
        mPath.lineTo(mDatumPoint.x, mDatumPoint.y - triangularLength / 2);
        mPath.lineTo(mDatumPoint.x - triangularLength / 2, mDatumPoint.y);
        mPath.close();
        canvas.drawPath(mPath, mBorderPaint);
    }

    private void drawRightTriangle(Canvas canvas) {
        int triangularLength = getPaddingRight();
        if (triangularLength == 0) {
            return;
        }

        mPath.addRoundRect(mRect, mRadius, mRadius, Path.Direction.CCW);
        mPath.moveTo(mDatumPoint.x, mDatumPoint.y - triangularLength / 2);
        mPath.lineTo(mDatumPoint.x + triangularLength / 2, mDatumPoint.y);
        mPath.lineTo(mDatumPoint.x, mDatumPoint.y + triangularLength / 2);
        mPath.close();
        canvas.drawPath(mPath, mBorderPaint);
    }

    private void drawBottomTriangle(Canvas canvas) {
        int triangularLength = getPaddingBottom();
        if (triangularLength == 0) {
            return;
        }

        mPath.addRoundRect(mRect, mRadius, mRadius, Path.Direction.CCW);
        mPath.moveTo(mDatumPoint.x + triangularLength / 2, mDatumPoint.y);
        mPath.lineTo(mDatumPoint.x, mDatumPoint.y + triangularLength / 2);
        mPath.lineTo(mDatumPoint.x - triangularLength / 2, mDatumPoint.y);
        mPath.close();
        canvas.drawPath(mPath, mBorderPaint);
    }

    @Override
    protected void onSizeChanged(int w, int h, int oldw, int oldh) {
        super.onSizeChanged(w, h, oldw, oldh);

        mRect.left = getPaddingLeft();
        mRect.top = getPaddingTop();
        mRect.right = w - getPaddingRight();
        mRect.bottom = h - getPaddingBottom();

        switch (mDirection) {
            case LEFT:
                mDatumPoint.x = getPaddingLeft();
                mDatumPoint.y = h / 2;
                break;
            case TOP:
                mDatumPoint.x = w / 2;
                mDatumPoint.y = getPaddingTop();
                break;
            case RIGHT:
                mDatumPoint.x = w - getPaddingRight();
                mDatumPoint.y = h / 2;
                break;
            case BOTTOM:
                mDatumPoint.x = w / 2;
                mDatumPoint.y = h - getPaddingBottom();
                break;
        }

        if (mOffset != 0) {
            applyOffset();
        }
    }

    /**
     * 设置三角形偏移位置
     *
     * @param offset 偏移量
     */
    public void setTriangleOffset(int offset) {
        this.mOffset = offset;
        applyOffset();
        invalidate();
    }

    private void applyOffset() {
        switch (mDirection) {
            case LEFT:
            case RIGHT:
                mDatumPoint.y += mOffset;
                break;
            case TOP:
            case BOTTOM:
                mDatumPoint.x += mOffset;
                break;
        }
    }
}
```





### 第02节 Activity 类

位置

```
com.cosmo.good.study.ui.activity.DemoActivity
```

代码

```java
import android.os.Bundle;

import androidx.annotation.Nullable;
import androidx.appcompat.app.AppCompatActivity;

import com.cosmo.good.study.R;

public class DemoActivity extends AppCompatActivity {


    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_demo);

    }
}
```





### 第03节 Activity 的布局文件

位置

```text
app/src/main/res/layout/activity_demo.xml
```

代码

```xml
<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <!--
        需要注意的是: 如果想要 气泡的三角和阴影生效, 必须设置足够的 padding
     -->
    <com.cosmo.good.study.common.view.CommonBubbleLayout
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:padding="20dp"
        app:bubble_background_color="#FF4081"
        app:bubble_direction="top"
        app:bubble_offset="100dp"
        app:bubble_radius="4dp"
        app:bubble_shadow_color="#999999"
        app:bubble_shadow_size="4dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            android:text="helloWorld"
            android:textColor="@color/white" />

    </com.cosmo.good.study.common.view.CommonBubbleLayout>


</FrameLayout>
```





### 第04节 属性文件

位置

```
app/src/main/res/values/attrs.xml
```

代码

```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        自定义的气泡布局效果
        1、气泡的背景颜色 bubble_background_color
        2、气泡的阴影颜色 bubble_shadow_color
        3、气泡的阴影大小 bubble_shadow_size
        4、气泡的圆角半径 bubble_radius
        5、气泡的三角方向 bubble_direction
        6、气泡的三角偏移量 bubble_offset
      -->
    <declare-styleable name="BubbleLayout">
        <attr name="bubble_background_color" format="color" />
        <attr name="bubble_shadow_color" format="color" />
        <attr name="bubble_shadow_size" format="dimension" />
        <attr name="bubble_radius" format="dimension" />
        <attr name="bubble_direction" format="enum">
            <enum name="left" value="1" />
            <enum name="top" value="2" />
            <enum name="right" value="3" />
            <enum name="bottom" value="4" />
        </attr>
        <attr name="bubble_offset" format="dimension" />
    </declare-styleable>


</resources>
```















